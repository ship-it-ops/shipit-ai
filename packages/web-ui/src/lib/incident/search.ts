// Incident Mode is about reaching a service fast — pipelines and monitors
// route to other surfaces and just add noise to the responder's autocomplete.
export const INCIDENT_SEARCH_EXCLUDE = ['Pipeline', 'Monitor'] as const;
